export const USER_MENTION_REGEX = /\[([^\]]+)\]\(user:([^)]+)\)/g;
export const ITEM_TAG_REGEX = /\[([^\]]+)\]\(item:([^)]+)\)/g;
export const COMBINED_REGEX = /\[([^\]]+)\]\((user|item):([^)]+)\)/g;
