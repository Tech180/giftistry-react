/** Delay before sending "stopped typing" over the WebSocket after the last keystroke. */
export const COMMENT_TYPING_STOP_DELAY_MS = 2000;

/** How long to show another user's typing indicator without a refresh. */
export const COMMENT_TYPING_USER_TTL_MS = 5000;
