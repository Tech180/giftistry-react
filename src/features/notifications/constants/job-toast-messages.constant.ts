export const JOB_TOAST_MESSAGES = {
  failed: 'Background item job failed.',
  softFailure: 'Product details were found, but AI summarization has failed.',
  completed: 'Background item job finished.',
} as const;
