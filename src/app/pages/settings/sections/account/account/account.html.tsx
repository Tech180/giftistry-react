import React from 'react';
import type { AccountTemplateProps } from './interfaces/template-props.interface';

export const AccountTemplate: React.FC<AccountTemplateProps> = ({ children }) => <>{children}</>;
