/** Viewport width where the add drawer shifts layout instead of overlaying the list. */
export const OVERLAY_BREAKPOINT = '75rem';

export const OVERLAY_BREAKPOINT_MEDIA_QUERY = `(min-width: ${OVERLAY_BREAKPOINT})`;
