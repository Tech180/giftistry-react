/** Horizontal drag (px) past which a left swipe closes the drawer. */
export const SWIPE_CLOSE_DISTANCE_PX = 100;

/** Velocity (px/ms) above which a left swipe closes the drawer. */
export const SWIPE_CLOSE_VELOCITY = 0.5;
