# `shared/utils`

Exported pure functions (and tiny private helpers).

## Allowed / forbidden

- **Allowed:** runtime helpers
- **Forbidden:** exported interfaces/types, shared const tables — put those in `shared/interfaces/` and `shared/constants/`

Utils import types from `interfaces/` and values from `constants/`.

## Related

- [↑ shared](../README.md)
